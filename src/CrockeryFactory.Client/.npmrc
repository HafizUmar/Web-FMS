# npm 10's peer-dependency resolver crashes on the vitest/jsdom peer set Angular 21
# scaffolds ("Cannot read properties of null (reading 'edgesOut')"). This flag sidesteps
# it so a plain `npm install` works. Remove it once the toolchain moves to npm 11+.
legacy-peer-deps=true
